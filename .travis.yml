env:
  matrix:
    - NETBSD_VERSION=6.0
    - NETBSD_VERSION=6.0.5
    - NETBSD_VERSION=6.1
    - NETBSD_VERSION=6.1.5
    - NETBSD_VERSION=7.0
    - NETBSD_VERSION=7.1.2
    - NETBSD_VERSION=8.0
  global:
    - PATH=$HOME/.local/bin:$PATH

sudo: required

language: perl

perl:
  - "5.22"

services:
- docker

install: true
script: true

jobs:
  include:
    - stage: build
      script:
        - echo "Building ${NETBSD_VERSION}"
    - stage: test
      script:
        - echo "Testing ${NETBSD_VERSION}"
    - stage: publish
      script:
        - echo "Publishing ${NETBSD_VERSION}"

deploy:
  provider: script
  script: echo "Deploying ${NETBSD_VERSION}"

